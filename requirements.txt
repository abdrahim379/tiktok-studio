streamlit
yt-dlp
requests
pillow
numpy
groq
piexif
imageio-ffmpeg
psycopg2-binary

# Extract Text needs faster-whisper, which pulls onnxruntime + av (~160 MB)
# and wants ~1.5 GB RAM for the medium model — past what Streamlit Cloud
# allows. Install it locally instead:  pip install faster-whisper
